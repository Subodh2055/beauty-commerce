# config

Shared tooling configuration.

- `ruff.toml` — Python lint/format rules (extended by `apps/api/pyproject.toml`).
- `.editorconfig` lives at the repo root.

Add shared ESLint/Prettier presets here when a second JS app appears.
